[project]
name = "c8module"
version = "0.1.0"
description = "Lokales Modul for Exercise C.9"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
